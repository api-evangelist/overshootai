openapi: 3.1.0
info:
  title: Inference Service Billing Metrics API
  version: 0.4.0
  description: Pricing, prepaid balance, and checkout.
tags:
- name: Metrics
paths:
  /metrics:
    get:
      summary: Metrics
      operationId: metrics_metrics_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
      tags:
      - Metrics
components:
  securitySchemes:
    API Key:
      type: http
      description: Bearer <api_key>
      scheme: bearer
